# Environment variables for SNA Evaluation Framework

# Azure OpenAI Configuration (for PyRIT scorers)
OPENAI_CHAT_ENDPOINT=https://api-ai.digitaldev.nl/openai/deployments/gpt-5/chat/completions?api-version=2024-04-01-preview
OPENAI_API_KEY=your-azure-openai-api-key

# Unity Catalog Configuration (for storing evaluation results)
DATABRICKS_SERVER_HOSTNAME=your-databricks-workspace.cloud.databricks.com
DATABRICKS_HTTP_PATH=/sql/1.0/warehouses/your-warehouse-id
DATABRICKS_TOKEN=your-databricks-access-token

# Note: Unity Catalog will write results to:
# - Table: ca_workspace.sna_evaluation.eval_results
# - Volume: ca_workspace.sna_evaluation.eval_reports
# 
# The API will automatically create the schema, table, and volume if they don't exist.
